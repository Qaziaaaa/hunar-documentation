{
  "name": "hunar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,green \"npm run start:dev --prefix hunar-backend\" \"npm run dev --prefix hunar-frontend -- -p 3001\"",
    "dev:backend": "npm run start:dev --prefix hunar-backend",
    "dev:frontend": "npm run dev --prefix hunar-frontend -- -p 3001",
    "install:all": "npm install --prefix hunar-backend && npm install --prefix hunar-frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}